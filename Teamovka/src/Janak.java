
import java.util.*;

public class Janak {
	Scanner sc = new scanner(System.in);	//vyžádání hesla
	System.out.printl("Zadej heslo:"); 
	string a = sc.nextLine();
	
	int ascii = (int) character;

	String character = name.StringAt(0);
	int ascii = (int) character; // ascii(heslo) je teï v decimálním kodu
	
	System.out.print(Integer.toBinaryString(ascii));	//pøevod z decimálního do binárního

}